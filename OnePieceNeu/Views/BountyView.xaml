<UserControl x:Class="OnePieceNeu.Views.BountyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OnePieceNeu.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/OPBackGround2.jpg" Stretch="Fill" AlignmentY="Top" /> 
        </Grid.Background>
        <Grid Background="Transparent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Bounty" 
                       HorizontalAlignment="Center" 
                       FontSize="36" 
                       FontWeight="Bold" 
                       Foreground="DarkRed" 
                       Margin="0,30,0,20" 
                       Grid.Row="0"/>

            <DataGrid ItemsSource="{Binding BountyListe}" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      Background="#FFF1F19F" 
                      RowBackground="Transparent" 
                      AlternatingRowBackground="#33FFFFF9" 
                      BorderBrush="DarkRed" 
                      BorderThickness="2"
                      FontSize="20" 
                      Margin="50,10,50,20" 
                      Grid.Row="1"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="DarkRed">

            <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="DarkRed"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="22"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        <Setter Property="BorderBrush" Value="DarkRed"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Spielername" 
                                        Binding="{Binding SpielerName}" 
                                        Foreground="DarkRed"
                                        Width="*" 
                                        FontWeight="DemiBold">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="Black"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Erreichte Punkte" 
                                        Binding="{Binding Score}" 
                                        Width="200">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="DarkRed"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>

            <Button Grid.Row="2" Command="{Binding ZurueckCommand}" Content="Zurück zum Hauptmenü" FontWeight="DemiBold" BorderBrush="DarkRed" Background="#FFF1F19F" Foreground="DarkRed" FontSize="24" Margin="0,0,0,30" Height="60" Width="300"></Button>

        </Grid>
    </Grid>
</UserControl>
