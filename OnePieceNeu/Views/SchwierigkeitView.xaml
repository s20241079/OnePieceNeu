<UserControl x:Class="OnePieceNeu.Views.SchwierigkeitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OnePieceNeu.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <DockPanel.Background>
            <ImageBrush ImageSource="/assets/OPBackground2.jpg" Stretch="UniformToFill" />
        </DockPanel.Background>
        <TextBlock Name="Header" Text="Schwierigkeitsgrad" DockPanel.Dock="Top" FontWeight="Bold" FontSize="50" HorizontalAlignment="Center" Foreground="DarkRed" Margin="0,20,0,0"/>

        <StackPanel Margin="0,60,0,0" DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Center">
            <RadioButton Name="RadioLeicht" IsChecked="{Binding IsLeichtSelected}" Content="Leicht" FontWeight="DemiBold" Foreground="DarkRed" Margin="40,0,40,0" Width="57" Background="#FFF1F19F">
                <RadioButton.LayoutTransform>
                    <ScaleTransform ScaleX="3" ScaleY="3"/>
                </RadioButton.LayoutTransform>
            </RadioButton>
            <RadioButton Name="RadioMittel" IsChecked="{Binding IsMittelSelected}" Content="Mittel" FontWeight="DemiBold" Foreground="DarkRed" VerticalAlignment="Center" Margin="40,0,40,0" Width="57" Background="#FFF1F19F">
                <RadioButton.LayoutTransform>
                    <ScaleTransform ScaleX="3" ScaleY="3"/>
                </RadioButton.LayoutTransform>
            </RadioButton>
            <RadioButton Name="RadioSchwer" IsChecked="{Binding IsSchwerSelected}" Content="Schwer" FontWeight="DemiBold" Foreground="DarkRed" VerticalAlignment="Center" Margin="40,0,40,0" Width="57" Background="#FFF1F19F">
                <RadioButton.LayoutTransform>
                    <ScaleTransform ScaleX="3" ScaleY="3"/>
                </RadioButton.LayoutTransform>
            </RadioButton>
        </StackPanel>

        <DockPanel DockPanel.Dock="Bottom" LastChildFill="False" Margin="0,0,0,40">
            <Button Command="{Binding ZurückCommand}" DockPanel.Dock="Left" BorderBrush="DarkRed" Background="#FFF1F19F" Foreground="DarkRed" FontWeight="DemiBold" FontSize="30" Margin="40,0,0,0" Content="Zurück" Height="50" Width="120" />
            <Button Command="{Binding WeiterCommand}" DockPanel.Dock="Right" BorderBrush="DarkRed" Background="#FFF1F19F" Foreground="DarkRed" FontWeight="DemiBold" FontSize="30" Margin="0,0,40,0" Content="Weiter" Height="50" Width="120" />
        </DockPanel>
    </DockPanel>
</UserControl>
